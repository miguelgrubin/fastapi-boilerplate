# Traefik Dynamic Configuration
# TLS certificates for local development
# Authentication is now handled at the application level via OIDC

tls:
  certificates:
    - certFile: '/certs/local.crt'
      keyFile: '/certs/local.key'
